#!/usr/bin/make -f

override_dh_auto_build:
	dh_auto_build -- install-dependencies

override_dh_builddeb:
	dh_builddeb -- -Zgzip

override_dh_installsystemd:
	dh_installsystemd --name=embassy-init --no-start
	dh_installsystemd --name=embassyd --no-start

%:
	dh $@